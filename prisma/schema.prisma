enum Role {
    ADMIN
    USER
}

model User {
    id       String @id
    email    String
    password String
    role     Role
    posts    Post[]
}

model Post {
    id          String  @id
    title       String
    description String?
    user        User    @relation(fields: [userId], references: [id])
    userId      String
}
